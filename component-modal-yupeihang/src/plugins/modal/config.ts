// 全局默认配置
import { ConfigType } from './modal.type'
const config: ConfigType = {
  // 是否显示右上角的关闭按钮，默认开启
  close: true,
  // 点击蒙层是否允许关闭，默认开启
  maskClose: true,
  confirmText: '确定',
  cancelText: '取消'
}
export default config